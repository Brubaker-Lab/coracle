#' Partial correlation of two data frames with a third data frame of covariates.
#'
#' @param x_data A data frame with at least one shared index column and at least one column of numeric data.
#' @param y_data A data frame with at least one shared index column and at least one column of numeric data.
#' @param z_data A data frame with at least one shared index column and at least one column of numeric data.
#' @param ...
#'
#' @return A data frame of partial correlation results.
#' @export
#'
#' @examples
#' pcor_col(df1, df2, df3)
pcor_col <- function(x_data, y_data, z_data, ..., x_name, y_name, z_name){

}
